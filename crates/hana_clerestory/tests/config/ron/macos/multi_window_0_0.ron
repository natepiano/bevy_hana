(
    version: 6,
    entries: [
        (
            key: Primary,
            state: (
                position: MonitorOffset((100, 100)),
                logical_width: 600,
                logical_height: 400,
                target: Classified(${MONITOR_0_DEVICE_KEY}),
                mode: Windowed,
                app_name: "restore_window",
            )
        ),
        (
            key: Managed("window-1"),
            state: (
                position: MonitorOffset((400, 300)),
                logical_width: 600,
                logical_height: 400,
                target: Classified(${MONITOR_0_DEVICE_KEY}),
                mode: Windowed,
                app_name: "restore_window",
            )
        ),
    ],
)
